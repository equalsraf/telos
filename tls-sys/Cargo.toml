[package]
name = "tls-sys"
version = "0.1.0"
authors = ["raf"]
license = "ISC"
description = "FFI bindings for libressl's libtls"
repository = "https://github.com/equalsraf/telos"

build = "src/build.rs"

[dependencies]
libc = "0.2"

[build-dependencies]
gcc = "0.3"

